# EAS Build ignore file
# Same as .gitignore but WITHOUT .env exclusions,
# so Firebase env vars are available in cloud builds

node_modules/
.expo/
.DS_Store
*.log
dist/

# Keep .env.local out (contains sensitive Sentry token)
.env.local
